import { describe, test } from "vitest";
import { setup } from "@nuxt/test-utils";

describe("Placeholder tests", async () => {
  await setup({
    // test context options
  });

  test("always passes", () => {
    // ...
  });
});
